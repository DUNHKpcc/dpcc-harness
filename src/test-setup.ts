// Vitest global setup. Importing the i18n module auto-initializes i18next
// (default language "en"), so component tests render real English copy instead
// of raw translation keys.
import "@/i18n";
